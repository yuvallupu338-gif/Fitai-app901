/*
 * tree.web.js — the Web Development tree.
 *
 * Thirty-two skills from "what is a browser" to deployment, with the
 * dependency edges the brief asks for: React needs JavaScript at 3, HTML at 3
 * and CSS at 2, not a single parent.
 *
 * Activity content is real. The quizzes have right answers and wrong answers
 * that are wrong for a reason — the distractors are the misconceptions people
 * actually hold, so getting one wrong tells the learner something. The code
 * challenges carry test cases that are executed against the submitted function
 * (see domain/verify.js), which is what lets this tree grade honestly with no
 * AI key present.
 *
 * `why` is not marketing copy. It answers "why am I being made to learn this",
 * which is the question that decides whether someone finishes a branch.
 */

/* Small helpers so the data below stays readable rather than drowning in
 * repeated object keys. */
const q = (prompt, options, answer, explain) => ({ prompt, options, answer, explain });

export const WEB_TREE = {
  id: 'web',
  name: 'Web Development',
  tagline: 'From a blank file to a deployed app.',
  category: 'Programming',
  icon: 'code',
  skills: [
    {
      id: 'internet_basics',
      name: 'How the Web Works',
      category: 'Foundations',
      difficulty: 1,
      estimatedHours: [1, 2],
      requires: [],
      description: 'Clients, servers, DNS, HTTP and what actually happens when you type a URL.',
      why: 'Every bug you will ever chase lives somewhere in this pipeline. Knowing the shape of it turns "the site is broken" into a question you can answer.',
      activities: [
        {
          id: 'internet_basics.learn',
          kind: 'learn',
          title: 'The request/response cycle',
          body: [
            'Typing a URL starts a chain. Your browser asks DNS to turn a name like example.com into an IP address. It opens a TCP connection to that address, usually wrapped in TLS so the traffic is encrypted, then sends an HTTP request: a method (GET), a path (/about), and headers describing what it wants.',
            'The server answers with a status code, headers, and usually a body. 200 means here it is. 301 and 302 mean it moved. 404 means no such path. 500 means the server broke while trying.',
            'The browser reads the HTML body and finds references to more things — stylesheets, scripts, images — and requests each of those too. A page is not one download; it is dozens.',
            'Two ideas carry a surprising amount of weight later. HTTP is stateless: the server does not remember your last request, which is why cookies and tokens exist. And the client is untrusted: anything the browser sends can be forged, which is why validation that only happens in the browser is decoration, not security.',
          ],
        },
        {
          id: 'internet_basics.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('A page returns 404. What does that tell you?',
              ['The server was reached and had no such path', 'The server is down', 'Your internet is disconnected', 'The page is broken internally'],
              0,
              'A 404 is a real answer from a working server. Down would give you a connection error; a broken page gives 500.'),
            q('Why do cookies and tokens exist?',
              ['HTTP is stateless, so the server has no memory of you between requests', 'They make requests faster', 'They encrypt the connection', 'Browsers require them'],
              0,
              'Each request arrives with no memory of the last one. Anything the server needs to know about you has to be re-sent every time.'),
            q('You validate an email field in JavaScript before submitting. Is the server safe from bad emails?',
              ['No — anyone can send a request without using your page at all', 'Yes, the browser blocks invalid submissions', 'Yes, as long as the field is required', 'Only if you also use a regex'],
              0,
              'Client validation is a courtesy to the user. The server has to assume every request is hostile, because it can be.'),
          ],
        },
      ],
    },

    {
      id: 'html_basics',
      name: 'HTML Basics',
      category: 'Frontend',
      difficulty: 1,
      estimatedHours: [2, 4],
      requires: [{ skillId: 'internet_basics', minLevel: 1 }],
      description: 'Elements, attributes, nesting, and the structure of a document.',
      why: 'HTML is the thing that actually exists on the page. CSS and JavaScript only ever decorate and manipulate what HTML declared.',
      activities: [
        {
          id: 'html_basics.learn',
          kind: 'learn',
          title: 'Elements and structure',
          body: [
            'An HTML document is a tree. Elements open with a tag, close with a matching tag, and contain either text or more elements. <p>Hello</p> is an element with text; <div><p>Hello</p></div> is an element containing another.',
            'Attributes go in the opening tag and configure the element: <a href="/about">About</a>. Some elements never have children and never close — <img src="cat.jpg" alt="A cat"> and <input type="text"> are the ones you meet first.',
            'Nesting must be strictly hierarchical. <b><i>text</b></i> is invalid; browsers will guess, and the guess will surprise you. Close in reverse order of opening.',
            'The alt attribute on an image is not optional in practice. It is what screen readers announce and what shows when the image fails, and an empty alt="" is the correct answer for a purely decorative image — not a missing one.',
          ],
        },
        {
          id: 'html_basics.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('Which is correctly nested?',
              ['<div><p>Hi</p></div>', '<div><p>Hi</div></p>', '<p><div>Hi</p></div>', '<div><p>Hi</div>'],
              0,
              'Elements close in the reverse order they opened. The inner element must close before the outer one.'),
            q('An image is purely decorative. What is the right alt attribute?',
              ['alt="" — empty, so screen readers skip it', 'Leave alt off entirely', 'alt="decorative image"', 'alt="image"'],
              0,
              'An empty alt tells assistive tech "nothing to say here". A missing alt makes it read the filename instead.'),
            q('What does an attribute do?',
              ['Configures the element it sits on', 'Adds text to the page', 'Styles the element', 'Creates a new element'],
              0,
              'Attributes are configuration: where a link points, which file an image loads, what type an input is.'),
          ],
        },
      ],
    },

    {
      id: 'semantic_html',
      name: 'Semantic HTML',
      category: 'Frontend',
      difficulty: 2,
      estimatedHours: [2, 3],
      requires: [{ skillId: 'html_basics', minLevel: 2 }],
      description: 'Using the element that means the thing, and why div soup costs you later.',
      why: 'Semantics are what make a page navigable by keyboard and screen reader for free, and they are most of the SEO advice you will ever be sold.',
      activities: [
        {
          id: 'semantic_html.learn',
          kind: 'learn',
          title: 'The element that means the thing',
          body: [
            'A <div> means nothing. It is a box. <header>, <nav>, <main>, <article>, <aside> and <footer> mean something, and assistive technology uses that meaning to offer navigation — jump to main content, list all headings, skip the nav.',
            'The biggest single win is using <button> for things that do something and <a href> for things that go somewhere. A <div onclick> looks identical and is invisible to keyboards: it cannot be tabbed to, Enter and Space do nothing, and screen readers do not announce it as interactive.',
            'Headings are an outline, not a font size. <h1> through <h6> should descend without skipping, because that outline is how many users navigate. If a heading is the wrong size, that is what CSS is for.',
          ],
        },
        {
          id: 'semantic_html.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('You need a clickable thing that opens a modal. What element?',
              ['<button>', '<div onclick>', '<a href="#">', '<span role="link">'],
              0,
              'It performs an action rather than navigating, so it is a button — and buttons are keyboard-operable and announced correctly with no extra work.'),
            q('Why avoid skipping from <h1> to <h3>?',
              ['The heading outline is a navigation structure for screen reader users', 'It is a syntax error', 'Browsers render it wrong', 'It breaks CSS selectors'],
              0,
              'Many users navigate by jumping between headings. A gap in the outline reads as a missing section.'),
            q('What does <main> give you that <div id="main"> does not?',
              ['A landmark assistive tech can jump straight to', 'Better default styling', 'Faster rendering', 'Automatic centring'],
              0,
              'Landmarks are navigable. "Skip to main content" works because <main> means something.'),
          ],
        },
      ],
    },

    {
      id: 'html_forms',
      name: 'Forms',
      category: 'Frontend',
      difficulty: 2,
      estimatedHours: [2, 4],
      requires: [{ skillId: 'semantic_html', minLevel: 2 }],
      description: 'Inputs, labels, validation and how form data actually reaches a server.',
      why: 'Forms are where users hand you data, and where most accessibility and security mistakes are made.',
      activities: [
        {
          id: 'html_forms.learn',
          kind: 'learn',
          title: 'Inputs, labels and submission',
          body: [
            'Every input needs a label, and the connection must be explicit: <label for="email">Email</label><input id="email">. Placeholder text is not a label — it disappears the moment someone types, and it is invisible to some assistive tech.',
            'The name attribute is what the server sees. An input without a name is not submitted at all, which is a genuinely confusing bug the first time you hit it.',
            'type gives you free validation and the right mobile keyboard: type="email" shows an @ key, type="number" shows digits. The browser will refuse to submit an invalid required field — useful, and still not a substitute for checking on the server.',
            'A button inside a form submits it by default. If you want a button that does something else, say type="button" explicitly, or you will spend twenty minutes wondering why the page reloads.',
          ],
        },
        {
          id: 'html_forms.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('An input submits nothing to the server. Most likely cause?',
              ['It has no name attribute', 'It has no id', 'It has no placeholder', 'It is not inside a div'],
              0,
              'name is the key the value is submitted under. No name, no key, no data.'),
            q('Why is a placeholder not a label?',
              ['It vanishes when the user types, leaving no indication of what the field was', 'It is styled differently', 'It cannot be translated', 'It only works on text inputs'],
              0,
              'The moment someone starts typing, the only description of the field is gone — worst for exactly the users who most need it.'),
            q('Your button inside a form reloads the page unexpectedly. Fix?',
              ['Give it type="button"', 'Add return false to onclick', 'Move it outside the form', 'Use a div instead'],
              0,
              'Buttons in forms default to type="submit". Saying type="button" is the direct fix.'),
          ],
        },
      ],
    },

    {
      id: 'css_basics',
      name: 'CSS Basics',
      category: 'Frontend',
      difficulty: 1,
      estimatedHours: [2, 4],
      requires: [{ skillId: 'html_basics', minLevel: 2 }],
      description: 'Rules, properties, the cascade and how a browser decides what wins.',
      why: 'Most CSS frustration is not about properties. It is about not knowing which rule won and why.',
      activities: [
        {
          id: 'css_basics.learn',
          kind: 'learn',
          title: 'The cascade',
          body: [
            'A CSS rule is a selector and a block of declarations. When two rules set the same property on the same element, the winner is decided in order: importance, then specificity, then source order.',
            'Specificity counts three things: ids, classes (and attributes and pseudo-classes), and elements. #nav is one id and beats .nav.active, which is two classes, which beats a stack of element selectors. An inline style beats all of them.',
            'Source order only breaks a tie between rules of equal specificity — the later one wins. This is why "I moved my stylesheet and everything changed" happens.',
            '!important skips the whole system, which is why reaching for it feels good and hurts later. If a rule needs !important, usually the real problem is a selector that is more specific than it needed to be.',
          ],
        },
        {
          id: 'css_basics.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('#header .title and .page .title both set colour. Which wins?',
              ['#header .title — an id outranks any number of classes', '.page .title — it comes later', 'Whichever is in the last stylesheet', 'Neither; they conflict and are ignored'],
              0,
              'Specificity is compared before source order, and one id beats any quantity of classes.'),
            q('Two rules have identical specificity. Which applies?',
              ['The one written later', 'The one written first', 'The shorter one', 'Both merge'],
              0,
              'Source order is the tiebreak, and only the tiebreak.'),
            q('You keep needing !important to override your own styles. What does that suggest?',
              ['Your selectors are more specific than they need to be', 'CSS is broken', 'You need more classes', 'You should use inline styles'],
              0,
              'Specificity wars are self-inflicted. Flatter selectors are the cure; !important is the symptom.'),
          ],
        },
      ],
    },

    {
      id: 'css_box_model',
      name: 'Box Model',
      category: 'Frontend',
      difficulty: 2,
      estimatedHours: [1, 3],
      requires: [{ skillId: 'css_basics', minLevel: 2 }],
      description: 'Content, padding, border, margin — and why your 300px box is 340px wide.',
      why: 'This is the single most common source of "why is this layout off by a few pixels".',
      activities: [
        {
          id: 'css_box_model.learn',
          kind: 'learn',
          title: 'Why your box is the wrong size',
          body: [
            'Every element is a content area, wrapped in padding, wrapped in a border, wrapped in margin. By default, width sets the content area only — so width: 300px with padding: 20px and a 1px border occupies 342px.',
            'box-sizing: border-box changes width to mean the whole visible box, padding and border included. Almost every codebase sets it globally as the first rule in the stylesheet, because the default is surprising and the alternative is doing arithmetic forever.',
            'Margins are outside the border and are the one part that can collapse: two stacked elements with 20px bottom and 30px top margin end up 30px apart, not 50px. Vertical margins between siblings collapse to the larger of the two.',
          ],
        },
        {
          id: 'css_box_model.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('width: 200px, padding: 16px, border: 2px, default box-sizing. Total width?',
              ['236px', '200px', '218px', '232px'],
              0,
              '200 content + 16 padding each side + 2 border each side = 200 + 32 + 4 = 236.'),
            q('What does box-sizing: border-box do?',
              ['Makes width include padding and border', 'Removes padding', 'Adds a border', 'Stops margins collapsing'],
              0,
              'width becomes the size of the visible box, which is what most people assumed it meant all along.'),
            q('One div has margin-bottom: 20px, the next margin-top: 30px. Gap between them?',
              ['30px — adjacent vertical margins collapse to the larger', '50px', '20px', '25px'],
              0,
              'Collapsing takes the larger of the two, not the sum.'),
          ],
        },
      ],
    },

    {
      id: 'css_flexbox',
      name: 'Flexbox',
      category: 'Frontend',
      difficulty: 2,
      estimatedHours: [2, 4],
      requires: [{ skillId: 'css_box_model', minLevel: 2 }],
      description: 'One-dimensional layout: rows, columns, alignment and distribution.',
      why: 'Flexbox is how you centre things, space a navbar, and build most component-level layout without hacks.',
      activities: [
        {
          id: 'css_flexbox.learn',
          kind: 'learn',
          title: 'Main axis and cross axis',
          body: [
            'display: flex makes an element a flex container and lays its direct children along a main axis. flex-direction picks that axis: row (default, horizontal) or column (vertical).',
            'justify-content distributes along the main axis. align-items aligns along the cross axis, the perpendicular one. This is the whole trick, and it is why the two properties appear to swap behaviour when you change flex-direction — they did not swap, the axes did.',
            'flex: 1 on a child means "take a share of the leftover space". Three children with flex: 1 split it equally. One with flex: 2 takes double.',
            'Centring both ways is justify-content: center plus align-items: center. That is the answer to the oldest question in CSS.',
          ],
        },
        {
          id: 'css_flexbox.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('flex-direction: column. Which property centres children horizontally?',
              ['align-items — horizontal is now the cross axis', 'justify-content', 'text-align', 'margin: auto'],
              0,
              'With a column direction the main axis is vertical, so horizontal alignment is the cross axis, which is align-items.'),
            q('Three children, all flex: 1. What happens?',
              ['They share the free space equally', 'They stack vertically', 'The first takes all the space', 'Nothing without a width'],
              0,
              'flex: 1 each means an equal share of whatever is left after content.'),
            q('justify-content: space-between with three items. Where do they sit?',
              ['First at the start, last at the end, one in the middle, gaps equal', 'All in the centre with equal gaps', 'Evenly spaced including outer gaps', 'All at the start'],
              0,
              'space-between puts no space at the outer edges — that is what distinguishes it from space-around and space-evenly.'),
          ],
        },
      ],
    },

    {
      id: 'css_grid',
      name: 'CSS Grid',
      category: 'Frontend',
      difficulty: 3,
      estimatedHours: [3, 5],
      requires: [{ skillId: 'css_flexbox', minLevel: 2 }],
      description: 'Two-dimensional layout: rows and columns together.',
      why: 'Page-level layout is two-dimensional. Grid expresses it directly instead of nesting flex containers until it works.',
      activities: [
        {
          id: 'css_grid.learn',
          kind: 'learn',
          title: 'Tracks, fr, and auto-fit',
          body: [
            'display: grid plus grid-template-columns defines vertical tracks. The fr unit is a share of free space: 1fr 2fr makes the second column twice the first.',
            'repeat(auto-fit, minmax(220px, 1fr)) is the one line worth memorising. It creates as many columns as fit at a minimum of 220px, then stretches them to fill — a responsive card grid with no media queries at all.',
            'gap sets the space between tracks, and unlike margins it never collapses and never adds space at the outer edge. It works in flexbox too.',
            'The rule of thumb: grid when you are laying out both axes at once, flexbox when it is a single row or column. They are complements, not rivals.',
          ],
        },
        {
          id: 'css_grid.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('What does repeat(auto-fit, minmax(200px, 1fr)) do?',
              ['Fits as many ≥200px columns as possible, stretching them to fill', 'Creates exactly 200 columns', 'Makes one column 200px wide', 'Sets a maximum width of 200px'],
              0,
              'It is a responsive grid in one declaration — the column count follows the container width.'),
            q('grid-template-columns: 1fr 2fr. If the container is 600px with no gap?',
              ['200px and 400px', '300px and 300px', '100px and 200px', '600px and 1200px'],
              0,
              'fr units split the free space in proportion: one share and two shares out of three.'),
            q('When is flexbox the better choice than grid?',
              ['A single row or column whose sizes depend on content', 'Any layout with more than three items', 'Page-level layout', 'Anything responsive'],
              0,
              'One dimension and content-driven sizing is flexbox territory. Two dimensions with defined tracks is grid.'),
          ],
        },
      ],
    },

    {
      id: 'css_responsive',
      name: 'Responsive Design',
      category: 'Frontend',
      difficulty: 3,
      estimatedHours: [3, 5],
      requires: [
        { skillId: 'css_grid', minLevel: 2 },
        { skillId: 'semantic_html', minLevel: 2 },
      ],
      description: 'Fluid layouts, media queries, and designing for the small screen first.',
      why: 'Most of your users are on a phone. A layout that only works at 1440px is a layout that mostly does not work.',
      activities: [
        {
          id: 'css_responsive.learn',
          kind: 'learn',
          title: 'Mobile first',
          body: [
            'Write the small-screen layout as the base, then use min-width media queries to add complexity as space appears. The reverse — desktop first with max-width queries — means every override fights the one above it, and the phone gets the most complicated code path.',
            'The viewport meta tag is required: without <meta name="viewport" content="width=device-width, initial-scale=1"> a phone pretends to be 980px wide and scales your page down, and every media query you wrote is ignored.',
            'Prefer layouts that do not need breakpoints at all. Percentages, fr units, minmax, clamp() and flex-wrap handle a continuous range of widths; a media query only handles the two sides of one number.',
            'Touch targets need to be around 44px. A 24px icon button is comfortable with a mouse and genuinely difficult with a thumb.',
          ],
        },
        {
          id: 'css_responsive.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('Your media queries have no effect on a real phone. Most likely cause?',
              ['Missing viewport meta tag', 'Wrong units in the query', 'CSS file not loaded', 'The phone is too old'],
              0,
              'Without it the phone reports a fake 980px width and scales the result, so breakpoints below that never fire.'),
            q('Why is mobile-first preferred?',
              ['The base layout is the simplest and each query adds rather than undoes', 'Phones are faster', 'max-width queries are deprecated', 'It uses less CSS on desktop'],
              0,
              'Additive complexity is easier to reason about than a stack of overrides undoing each other.'),
            q('Roughly how large should a touch target be?',
              ['About 44px', 'About 20px', 'About 12px', 'Whatever the icon is'],
              0,
              'Anything much smaller becomes a game of accuracy for thumbs.'),
          ],
        },
      ],
    },

    {
      id: 'js_basics',
      name: 'JavaScript Basics',
      category: 'Programming',
      difficulty: 2,
      estimatedHours: [4, 8],
      requires: [{ skillId: 'html_basics', minLevel: 2 }],
      description: 'Variables, types, operators, control flow and the equality trap.',
      why: 'Everything else in this branch — the DOM, React, Node — is JavaScript with extra vocabulary.',
      activities: [
        {
          id: 'js_basics.learn',
          kind: 'learn',
          title: 'Values, bindings and equality',
          body: [
            'const binds a name to a value and stops you rebinding it. let allows rebinding. var is the old form with confusing scope rules; there is no reason to write it in new code. Default to const, switch to let only when you actually reassign.',
            'const on an object does not freeze the object. const user = {} still allows user.name = "Alex" — what is fixed is which object the name points at, not the contents.',
            'Use === and never ==. The double equals coerces types before comparing, which produces "" == 0 being true, "0" == 0 being true, and null == undefined being true while null === undefined is false. Triple equals compares without coercion and holds no surprises.',
            'Six values are falsy: false, 0, "", null, undefined and NaN. Everything else is truthy — including "0", "false", empty arrays and empty objects, all of which catch people out.',
          ],
        },
        {
          id: 'js_basics.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('What is [] == false?',
              ['true — both coerce toward 0', 'false', 'undefined', 'A TypeError'],
              0,
              'An empty array coerces to "" and then to 0, and false is 0. This is exactly the sort of result === avoids.'),
            q('const user = {name:"A"}; user.name = "B". What happens?',
              ['It works — const fixes the binding, not the contents', 'TypeError', 'Silently ignored', 'The object is copied'],
              0,
              'const prevents reassigning the variable. Mutating the object it points to is untouched.'),
            q('Which of these is truthy?',
              ['"0"', '0', '""', 'NaN'],
              0,
              'A non-empty string is truthy regardless of what it contains. Only the six falsy values are falsy.'),
          ],
        },
        {
          id: 'js_basics.challenge',
          kind: 'challenge',
          title: 'FizzBuzz, without the ladder',
          brief: 'Write fizzBuzz(n) returning "Fizz" if n divides by 3, "Buzz" if by 5, "FizzBuzz" if by both, otherwise n as a string.',
          starter: 'function fizzBuzz(n) {\n  \n}',
          entry: 'fizzBuzz',
          tests: [
            { args: [3], expected: 'Fizz' },
            { args: [5], expected: 'Buzz' },
            { args: [15], expected: 'FizzBuzz' },
            { args: [7], expected: '7' },
            { args: [30], expected: 'FizzBuzz' },
            { args: [1], expected: '1' },
          ],
          hint: 'Check the both-case first, or build the string by appending — the order of your conditions is the whole exercise.',
        },
      ],
    },

    {
      id: 'js_functions',
      name: 'Functions',
      category: 'Programming',
      difficulty: 2,
      estimatedHours: [3, 5],
      requires: [{ skillId: 'js_basics', minLevel: 2 }],
      description: 'Parameters, return values, arrow functions, scope and closures.',
      why: 'Closures are the concept that separates people who use React hooks from people who fight them.',
      activities: [
        {
          id: 'js_functions.learn',
          kind: 'learn',
          title: 'Scope and closure',
          body: [
            'A function can see the variables of the scope it was written in. That is a closure: the function keeps hold of that scope even after the outer function has returned. It is not an advanced feature you opt into — every function you write is one.',
            'This is why a counter works: function makeCounter() { let n = 0; return () => ++n; }. The returned arrow still has n, and each call to makeCounter creates a fresh, private one.',
            'Arrow functions differ from function declarations in two ways that matter. They are not hoisted, so you cannot call one before its line. And they have no `this` of their own — they inherit it from the enclosing scope, which is usually what you wanted and is why they took over for callbacks.',
            'The classic bug: a loop with var creates one shared binding, so all the callbacks see the final value. let creates a fresh binding per iteration and fixes it.',
          ],
        },
        {
          id: 'js_functions.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('for (var i=0;i<3;i++) setTimeout(()=>console.log(i)). Output?',
              ['3, 3, 3', '0, 1, 2', '0, 0, 0', '2, 2, 2'],
              0,
              'var gives one binding shared by all three callbacks, and by the time they run the loop has finished with i at 3. Swap to let for 0,1,2.'),
            q('What is a closure?',
              ['A function keeping access to the scope it was defined in', 'A function that closes over itself', 'A private class method', 'A way to end a loop'],
              0,
              'It is the mechanism that lets a returned function still reach its parent variables.'),
            q('Why do arrow functions suit callbacks?',
              ['They inherit `this` instead of defining their own', 'They are faster', 'They are hoisted', 'They cannot be reassigned'],
              0,
              'No own `this` means no need for bind or a self variable when passing a method as a callback.'),
          ],
        },
        {
          id: 'js_functions.challenge',
          kind: 'challenge',
          title: 'Build a counter factory',
          brief: 'Write makeCounter(start) returning a function that returns start, then start+1, then start+2 on each successive call. Two counters must not share state.',
          starter: 'function makeCounter(start) {\n  \n}',
          entry: 'makeCounter',
          tests: [
            { call: 'const c = makeCounter(0); return [c(), c(), c()].join(",")', expected: '0,1,2' },
            { call: 'const a = makeCounter(5); const b = makeCounter(100); a(); return [a(), b()].join(",")', expected: '6,100' },
            { call: 'const c = makeCounter(-2); return [c(), c()].join(",")', expected: '-2,-1' },
          ],
          hint: 'Declare the counter variable inside makeCounter, not outside it — that is what makes each counter private.',
        },
      ],
    },

    {
      id: 'js_arrays',
      name: 'Arrays',
      category: 'Programming',
      difficulty: 2,
      estimatedHours: [3, 5],
      requires: [{ skillId: 'js_functions', minLevel: 2 }],
      description: 'map, filter, reduce, and which array methods mutate.',
      why: 'Rendering a list, filtering a search, totalling a cart — array transformation is most of what front-end code does.',
      activities: [
        {
          id: 'js_arrays.learn',
          kind: 'learn',
          title: 'Transform, do not mutate',
          body: [
            'map returns a new array of the same length with each item transformed. filter returns a new, shorter array of the items that passed a test. reduce folds the whole array into one value — a sum, an object, another array. Between them they replace most loops you would otherwise write.',
            'Knowing which methods mutate is not trivia; it is the source of a whole class of bug. sort, reverse, splice, push, pop, shift and unshift all change the array in place. map, filter, slice, concat and spread all return something new.',
            'sort mutates and, by default, sorts as strings — [10, 9, 1].sort() gives [1, 10, 9]. For numbers you need a comparator: sort((a, b) => a - b). And if the original array matters, copy first: [...list].sort(...).',
            'A Set is the shortest way to deduplicate: [...new Set(list)] preserves order and removes repeats.',
          ],
        },
        {
          id: 'js_arrays.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('[10, 9, 1].sort() returns?',
              ['[1, 10, 9] — sorted as strings', '[1, 9, 10]', '[10, 9, 1]', '[9, 10, 1]'],
              0,
              'Default sort compares string forms, and "10" sorts before "9". Pass a comparator for numbers.'),
            q('Which of these does NOT mutate the original array?',
              ['filter', 'sort', 'splice', 'reverse'],
              0,
              'filter returns a new array. The other three change the array in place.'),
            q('Shortest way to remove duplicates from an array?',
              ['[...new Set(arr)]', 'arr.filter(unique)', 'arr.sort().unique()', 'arr.reduce(dedupe)'],
              0,
              'A Set holds each value once, and spreading it back gives an array in first-seen order.'),
          ],
        },
        {
          id: 'js_arrays.challenge',
          kind: 'challenge',
          title: 'Remove duplicates',
          brief: 'Write unique(arr) returning a new array with duplicates removed, keeping first-seen order. Do not modify the input.',
          starter: 'function unique(arr) {\n  \n}',
          entry: 'unique',
          tests: [
            { args: [[1, 2, 2, 3, 1]], expected: [1, 2, 3] },
            { args: [[]], expected: [] },
            { args: [['a', 'b', 'a']], expected: ['a', 'b'] },
            { args: [[5, 5, 5, 5]], expected: [5] },
            { call: 'const src = [3,1,3]; unique(src); return src.join(",")', expected: '3,1,3' },
          ],
          hint: 'A Set does the work in one line — but check the last test: it verifies you left the input alone.',
        },
        {
          id: 'js_arrays.assessment',
          kind: 'assessment',
          title: 'Array transformation',
          brief: 'Write summarise(orders) taking [{item, qty, price}] and returning {total, count, items} where total is the summed qty×price, count is total quantity, and items is an array of item names with no duplicates, in first-seen order.',
          starter: 'function summarise(orders) {\n  \n}',
          entry: 'summarise',
          tests: [
            {
              args: [[{ item: 'pen', qty: 2, price: 1.5 }, { item: 'pad', qty: 1, price: 3 }]],
              expected: { total: 6, count: 3, items: ['pen', 'pad'] },
            },
            { args: [[]], expected: { total: 0, count: 0, items: [] } },
            {
              args: [[{ item: 'pen', qty: 1, price: 2 }, { item: 'pen', qty: 3, price: 2 }]],
              expected: { total: 8, count: 4, items: ['pen'] },
            },
          ],
          hint: 'One reduce can do all three, but three separate passes is perfectly good and easier to read.',
        },
      ],
    },

    {
      id: 'js_objects',
      name: 'Objects',
      category: 'Programming',
      difficulty: 2,
      estimatedHours: [2, 4],
      requires: [{ skillId: 'js_functions', minLevel: 2 }],
      description: 'Properties, destructuring, spread, and reference semantics.',
      why: 'Every API response and every piece of component state is an object. Copying one wrong is a bug you will hit within a week of using React.',
      activities: [
        {
          id: 'js_objects.learn',
          kind: 'learn',
          title: 'References and copies',
          body: [
            'Objects are held by reference. const b = a does not copy anything — both names point at the same object, and changing b.x changes a.x. This is the single most common source of "why did that other thing change".',
            '{...a} makes a shallow copy: the top level is new, but any nested object is still shared. For nested data you either copy each level you touch, or use structuredClone(a) for a deep copy.',
            'Destructuring pulls values out by name: const {name, age} = user. It works in parameter lists too, which is how most React components read their props.',
            'Optional chaining short-circuits on null or undefined: user?.address?.city gives undefined instead of throwing. Pair it with ?? for a default — user.name ?? "Anonymous" — which, unlike ||, does not swallow 0 and "".',
          ],
        },
        {
          id: 'js_objects.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('const b = a; b.x = 1. What is a.x?',
              ['1 — both names refer to the same object', 'undefined', '0', 'A TypeError'],
              0,
              'Assignment copies the reference, not the object.'),
            q('Why prefer ?? over || for defaults?',
              ['|| also replaces 0 and "" , which are often valid values', '?? is faster', '|| is deprecated', 'They are identical'],
              0,
              '?? only falls through on null and undefined, so a legitimate 0 survives.'),
            q('{...user} copies a nested address object how?',
              ['By reference — it is a shallow copy', 'Deeply, recursively', 'Not at all', 'As a frozen copy'],
              0,
              'Spread copies one level. Nested objects are still shared with the original.'),
          ],
        },
      ],
    },

    {
      id: 'js_dom',
      name: 'The DOM',
      category: 'Programming',
      difficulty: 2,
      estimatedHours: [3, 5],
      requires: [
        { skillId: 'js_basics', minLevel: 3 },
        { skillId: 'html_basics', minLevel: 3 },
      ],
      description: 'Selecting, creating and updating elements from JavaScript.',
      why: 'React exists to save you from doing this by hand — but when React misbehaves, this is the layer you debug at.',
      activities: [
        {
          id: 'js_dom.learn',
          kind: 'learn',
          title: 'Reading and changing the page',
          body: [
            'querySelector takes any CSS selector and returns the first match; querySelectorAll returns all of them as a static NodeList. The list does not update when the page changes — it is a snapshot.',
            'To add content, prefer textContent over innerHTML. innerHTML parses its input as markup, so any string that came from a user can carry a script tag or an event handler attribute. textContent inserts text as text, and the whole category of injection bug disappears.',
            'Building elements: document.createElement, set properties, appendChild. It is more code than a template string and it is safe by construction, which is the trade most codebases eventually make.',
            'Touching the DOM is slow relative to everything else. Changing fifty elements in a loop causes repeated layout work; build the whole subtree first, then append it once.',
          ],
        },
        {
          id: 'js_dom.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('Why prefer textContent to innerHTML for user-supplied text?',
              ['innerHTML parses markup, so hostile input can inject scripts', 'textContent is faster to type', 'innerHTML is deprecated', 'textContent supports more tags'],
              0,
              'Anything that renders user input as markup is an injection hole. textContent cannot be one.'),
            q('querySelectorAll returns what?',
              ['A static NodeList that does not update as the page changes', 'A live array', 'The first match only', 'A jQuery object'],
              0,
              'It is a snapshot. Elements added afterwards will not appear in it.'),
            q('You need to add 100 rows to a table. Best approach?',
              ['Build them off-document, then append once', 'Append each one as you create it', 'Use innerHTML += in a loop', 'Reload the page'],
              0,
              'Each append can trigger layout work. One insertion of a finished subtree does that work once.'),
          ],
        },
      ],
    },

    {
      id: 'js_events',
      name: 'Events',
      category: 'Programming',
      difficulty: 2,
      estimatedHours: [2, 4],
      requires: [{ skillId: 'js_dom', minLevel: 2 }],
      description: 'Listeners, the event object, bubbling and delegation.',
      why: 'Bubbling explains most "my click handler fired twice" bugs, and delegation is how you handle a list of a thousand items with one listener.',
      activities: [
        {
          id: 'js_events.learn',
          kind: 'learn',
          title: 'Bubbling and delegation',
          body: [
            'addEventListener(type, handler) attaches a listener. The handler receives an event object: target is the element that was actually clicked, currentTarget is the element the listener is on. They are different whenever the click landed on a child, and confusing them is a common bug.',
            'Events bubble: a click on a button inside a div fires the button listener, then the div listener, then up to document. preventDefault stops the browser default (following a link, submitting a form); stopPropagation stops the bubbling. They do different things and are often reached for interchangeably by mistake.',
            'Delegation uses bubbling deliberately: put one listener on the container and check event.target to see which child was hit. One listener for a list of any length, and it keeps working for items added later — which per-item listeners do not.',
          ],
        },
        {
          id: 'js_events.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('Listener on a div, user clicks a button inside it. What is event.target?',
              ['The button — the element actually clicked', 'The div', 'document', 'Both'],
              0,
              'target is where the event originated; currentTarget is where the listener sits.'),
            q('What does event delegation buy you?',
              ['One listener handles current and future children', 'It prevents bubbling', 'It runs handlers faster', 'It removes the need for event.target'],
              0,
              'Items added after the listener was attached are handled automatically, because the listener is on the parent.'),
            q('A link should not navigate when clicked. Which method?',
              ['preventDefault', 'stopPropagation', 'return true', 'stopImmediatePropagation'],
              0,
              'preventDefault cancels the browser behaviour. stopPropagation only stops the event travelling further up.'),
          ],
        },
      ],
    },

    {
      id: 'js_async',
      name: 'Async JavaScript',
      category: 'Programming',
      difficulty: 3,
      estimatedHours: [4, 7],
      requires: [
        { skillId: 'js_functions', minLevel: 3 },
        { skillId: 'js_objects', minLevel: 2 },
      ],
      description: 'Promises, async/await, error handling and the event loop.',
      why: 'Every network call is asynchronous. Without this, data arrives after your code looked for it and you get undefined forever.',
      activities: [
        {
          id: 'js_async.learn',
          kind: 'learn',
          title: 'Promises and await',
          body: [
            'JavaScript runs on one thread. Slow work — a network request, a timer — is handed off, and a callback is queued for when it finishes. The event loop runs those callbacks once the current code has finished, which is why code after a fetch runs before the response arrives.',
            'A promise represents a value that is not here yet. It is pending, then either fulfilled with a value or rejected with an error. await pauses the enclosing async function until it settles, letting you write sequential-looking code that is still non-blocking.',
            'An async function always returns a promise, even if you return a plain number. This is why calling one without await gives you a Promise object rather than the value — probably the most common async mistake.',
            'Handle rejection with try/catch around the await, or .catch() on the chain. An unhandled rejection fails silently in the browser, which turns a network error into a page that just never updates.',
            'For independent requests use Promise.all([a, b]) rather than awaiting each in turn: sequential awaits take the sum of the times, Promise.all takes the longest.',
          ],
        },
        {
          id: 'js_async.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('async function f() { return 1 }. What is f()?',
              ['A Promise resolving to 1', 'The number 1', 'undefined', 'An error'],
              0,
              'async functions always wrap their return value in a promise. You need await or .then to get the 1.'),
            q('Three independent fetches. What is fastest?',
              ['Promise.all of all three', 'Three sequential awaits', 'A for loop with await', 'Nested .then chains'],
              0,
              'Sequential awaits take the sum of the durations; Promise.all overlaps them and takes the longest single one.'),
            q('Why does code after a fetch call run before the response arrives?',
              ['The work is handed off and the callback queued until the current code finishes', 'fetch is synchronous', 'The response is cached', 'It does not'],
              0,
              'That is the event loop: the current call stack runs to completion before any queued callback does.'),
          ],
        },
        {
          id: 'js_async.challenge',
          kind: 'challenge',
          title: 'Retry with backoff',
          brief: 'Write retry(fn, times) that calls the async fn, and on rejection retries up to `times` more attempts before giving up. Return the resolved value, or reject with the last error.',
          starter: 'async function retry(fn, times) {\n  \n}',
          entry: 'retry',
          tests: [
            { call: 'let n = 0; return await retry(async () => { n++; if (n < 3) throw new Error("no"); return "ok"; }, 5)', expected: 'ok' },
            { call: 'let n = 0; try { await retry(async () => { n++; throw new Error("nope"); }, 2) } catch (e) { return n }', expected: 3 },
            { call: 'return await retry(async () => "first", 3)', expected: 'first' },
          ],
          hint: 'A loop with try/catch is clearer than recursion here. Note the second test: `times` retries means times+1 total attempts.',
        },
      ],
    },

    {
      id: 'js_apis',
      name: 'Working with APIs',
      category: 'Programming',
      difficulty: 3,
      estimatedHours: [3, 5],
      requires: [{ skillId: 'js_async', minLevel: 3 }],
      description: 'fetch, JSON, status codes, error handling and CORS.',
      why: 'This is the join between your interface and everybody else\'s data.',
      activities: [
        {
          id: 'js_apis.learn',
          kind: 'learn',
          title: 'fetch, and the trap in it',
          body: [
            'fetch returns a promise for a Response. Parsing the body is a second async step: await response.json(). Two awaits, always.',
            'The trap: fetch only rejects on network failure. A 404 or a 500 is a successful round trip as far as fetch is concerned, so the promise fulfils and your error handler never runs. You have to check response.ok yourself and throw.',
            'CORS is enforced by the browser, not the server: the server states which origins may read its responses, and the browser refuses if yours is not listed. You cannot fix a CORS error from the front end — it is fixed by the server sending the right headers, or by proxying through your own.',
            'Never put an API secret in front-end code. Anything the browser can read, a user can read — the network tab shows every request you make.',
          ],
        },
        {
          id: 'js_apis.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('fetch gets a 404. Does the promise reject?',
              ['No — check response.ok yourself', 'Yes, with a 404 error', 'Only if you pass a flag', 'Only for POST requests'],
              0,
              'fetch rejects on network failure only. An HTTP error status is still a completed request.'),
            q('You hit a CORS error. Where is the fix?',
              ['On the server, or via your own proxy — it is not fixable from the client', 'Add a header to your fetch call', 'Disable it in JavaScript', 'Use XMLHttpRequest instead'],
              0,
              'The browser enforces the server\'s policy. Client code cannot grant itself permission.'),
            q('Where should an API secret key live?',
              ['On a server the user cannot read', 'In a JS variable', 'In an environment variable bundled into the front end', 'In localStorage'],
              0,
              'Anything shipped to the browser is public, however it is stored. Bundled env vars included.'),
          ],
        },
      ],
    },

    {
      id: 'git',
      name: 'Git',
      category: 'Tools',
      difficulty: 2,
      estimatedHours: [3, 5],
      requires: [{ skillId: 'js_basics', minLevel: 2 }],
      description: 'Commits, branches, merges and undoing things safely.',
      why: 'Git is what makes experiments safe. Without it you keep folders called final-v2-real, and you will still lose work.',
      activities: [
        {
          id: 'git.learn',
          kind: 'learn',
          title: 'Snapshots, not diffs',
          body: [
            'A commit is a snapshot of the whole tree plus a pointer to its parent. Branches are just movable labels pointing at a commit — which is why creating one is instant and free, and why you should make them liberally.',
            'The three places a change can be: the working directory (edited), the staging area (git add), the repository (git commit). Staging exists so you can commit a coherent subset of your changes rather than everything you touched.',
            'Undo, from safest to sharpest: git restore <file> discards uncommitted edits; git revert <commit> makes a new commit undoing an old one and is safe on shared branches; git reset moves the branch pointer and rewrites history, which is fine locally and hostile if others have pulled it.',
            'A merge conflict is not a failure. It means two branches changed the same lines and git is asking which you meant. Edit the marked region, remove the markers, stage, commit.',
          ],
        },
        {
          id: 'git.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('You need to undo a commit already pushed to a shared branch. Which?',
              ['git revert — it adds a new commit undoing it, rewriting nothing', 'git reset --hard', 'Delete and re-push the branch', 'git checkout the old commit'],
              0,
              'Rewriting history others have pulled breaks their clones. revert is additive and safe.'),
            q('What is a branch?',
              ['A movable pointer to a commit', 'A copy of the project folder', 'A patch file', 'A remote server'],
              0,
              'That is why branching is instant regardless of repository size.'),
            q('What is the staging area for?',
              ['Choosing which changes go into the next commit', 'Backing up files', 'Sharing with other developers', 'Resolving conflicts'],
              0,
              'It lets one messy working directory produce several clean, coherent commits.'),
          ],
        },
      ],
    },

    {
      id: 'react_basics',
      name: 'React Basics',
      category: 'Frontend',
      difficulty: 3,
      estimatedHours: [5, 9],
      /* The brief's worked example, encoded literally. */
      requires: [
        { skillId: 'js_arrays', minLevel: 3 },
        { skillId: 'js_dom', minLevel: 3 },
        { skillId: 'css_flexbox', minLevel: 2 },
      ],
      description: 'Components, JSX and rendering a tree of UI from data.',
      why: 'React inverts how you think about the DOM: you describe what the page should look like for a given state, instead of scripting the steps to change it.',
      activities: [
        {
          id: 'react_basics.learn',
          kind: 'learn',
          title: 'Describing UI, not steps',
          body: [
            'A component is a function that takes props and returns a description of UI. React compares that description with the last one and applies the minimum set of DOM changes. You never write appendChild again.',
            'JSX is syntax sugar for function calls. className instead of class, camelCase event props, and any JavaScript expression inside braces: {items.length} works, an if statement does not — use a ternary or && instead.',
            'Rendering a list means mapping data to elements, and each needs a stable key. The key tells React which item is which across renders. Using the array index breaks the moment the list reorders or an item is removed — React reuses the wrong element and state ends up attached to the wrong row.',
            'Components must be pure during render: same props in, same output, no side effects. Mutating a prop, or fetching in the render body, produces bugs that only appear sometimes, which are the worst kind.',
          ],
        },
        {
          id: 'react_basics.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('Why is the array index a poor key for a reorderable list?',
              ['React matches elements by key, so reordering attaches state to the wrong item', 'Indexes are slow', 'Keys must be strings', 'It causes a full re-render'],
              0,
              'The key identifies an item across renders. If it is positional, moving an item silently reassigns its identity.'),
            q('Why can you not put an if statement inside JSX braces?',
              ['Braces take an expression, and if is a statement', 'React forbids conditionals', 'It breaks the compiler', 'You can'],
              0,
              'Ternaries and && are expressions and work; if is a statement and does not. Or compute it above the return.'),
            q('What does "components must be pure during render" rule out?',
              ['Fetching data or mutating props in the render body', 'Using props', 'Returning different JSX for different props', 'Calling other components'],
              0,
              'Same input must give same output with no side effects. Effects belong in useEffect or event handlers.'),
          ],
        },
      ],
    },

    {
      id: 'react_props',
      name: 'Props & Composition',
      category: 'Frontend',
      difficulty: 3,
      estimatedHours: [2, 4],
      requires: [{ skillId: 'react_basics', minLevel: 2 }],
      description: 'Passing data down, children, and composing small components.',
      why: 'Composition is what stops a codebase becoming five thousand-line components.',
      activities: [
        {
          id: 'react_props.learn',
          kind: 'learn',
          title: 'Data down, events up',
          body: [
            'Props flow one way: parent to child. A child cannot change its props — it calls a function the parent passed down, and the parent changes its own state. Data down, events up.',
            'children is the prop that holds whatever was nested inside your tag. It is what lets you write a <Card> that wraps arbitrary content without knowing anything about it, and it is the difference between components you configure with fifteen props and components you compose.',
            'When you find yourself threading a prop through four layers that do not use it, that is prop drilling — the signal to either restructure with composition or reach for context.',
          ],
        },
        {
          id: 'react_props.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('A child needs to update a value owned by its parent. How?',
              ['The parent passes a callback, the child calls it', 'The child assigns to props', 'Via a global variable', 'The child re-renders the parent'],
              0,
              'Props are read-only. Changes go up as events and come back down as new props.'),
            q('What is the children prop?',
              ['Whatever was nested inside the component tag', 'A list of child components in the file', 'The DOM children', 'An array of keys'],
              0,
              'It is what makes wrapper components like Card, Modal and Layout possible.'),
            q('You pass a prop through four components that never read it. This is?',
              ['Prop drilling — a sign to restructure or use context', 'Normal and fine', 'A performance optimisation', 'Required by React'],
              0,
              'It couples every layer to data it does not care about.'),
          ],
        },
      ],
    },

    {
      id: 'react_state',
      name: 'State',
      category: 'Frontend',
      difficulty: 3,
      estimatedHours: [4, 7],
      requires: [{ skillId: 'react_props', minLevel: 2 }],
      description: 'useState, immutable updates, and where state should live.',
      why: 'Nearly every React bug is a state bug: the wrong shape, the wrong place, or mutated instead of replaced.',
      activities: [
        {
          id: 'react_state.learn',
          kind: 'learn',
          title: 'Replace, never mutate',
          body: [
            'useState returns the current value and a setter. Calling the setter schedules a re-render; it does not change the variable you are holding. Reading it again on the next line gives the old value, which surprises everyone once.',
            'React decides whether to re-render by comparing references. items.push(x) then setItems(items) passes the same reference, so nothing happens. setItems([...items, x]) makes a new array and it works. The same applies to objects: {...user, name} rather than user.name = name.',
            'When the next value depends on the current one, use the function form: setCount(c => c + 1). Two calls in the same handler with the direct form both read the same stale value and you lose an increment.',
            'State belongs at the lowest common ancestor of everything that uses it — high enough to be shared, no higher. State parked at the top of the app re-renders everything beneath it for no reason.',
            'Do not put in state what you can derive from it. If you have items and a filter, the filtered list is a calculation during render, not a fourth piece of state that can fall out of sync.',
          ],
        },
        {
          id: 'react_state.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('items.push(x); setItems(items). Why does nothing re-render?',
              ['The reference is unchanged, so React sees no difference', 'push is async', 'You need two setState calls', 'It does re-render'],
              0,
              'Change detection is by reference. Mutating in place keeps the same array, so React concludes nothing changed.'),
            q('Two setCount(count + 1) calls in one handler. Result?',
              ['+1 — both read the same stale count', '+2', 'An error', 'Undefined behaviour'],
              0,
              'Both close over the same value. setCount(c => c + 1) reads the pending value and gives +2.'),
            q('You have items and a search term in state. Where should the filtered list live?',
              ['Derived during render, not in state', 'In its own useState', 'In a ref', 'In context'],
              0,
              'Derived state duplicates the truth and eventually disagrees with it.'),
          ],
        },
        {
          id: 'react_state.challenge',
          kind: 'challenge',
          title: 'Immutable update helpers',
          brief: 'Write toggleTodo(todos, id) returning a NEW array where the todo with that id has its `done` flipped. The original array and all its objects must be untouched.',
          starter: 'function toggleTodo(todos, id) {\n  \n}',
          entry: 'toggleTodo',
          tests: [
            {
              args: [[{ id: 1, done: false }, { id: 2, done: false }], 1],
              expected: [{ id: 1, done: true }, { id: 2, done: false }],
            },
            { call: 'const src = [{id:1, done:false}]; const out = toggleTodo(src, 1); return src[0].done === false && out[0].done === true', expected: true },
            { call: 'const src = [{id:1, done:false}]; return toggleTodo(src, 1)[0] === src[0]', expected: false },
            { args: [[{ id: 1, done: true }], 9], expected: [{ id: 1, done: true }] },
          ],
          hint: 'map plus a spread. The third test checks you replaced the object rather than editing it in place.',
        },
      ],
    },

    {
      id: 'react_hooks',
      name: 'Hooks & Effects',
      category: 'Frontend',
      difficulty: 4,
      estimatedHours: [5, 9],
      requires: [{ skillId: 'react_state', minLevel: 3 }, { skillId: 'js_async', minLevel: 3 }],
      description: 'useEffect, dependencies, cleanup and custom hooks.',
      why: 'Effects are where React meets the outside world, and where the subtlest bugs live.',
      activities: [
        {
          id: 'react_hooks.learn',
          kind: 'learn',
          title: 'Effects and their dependencies',
          body: [
            'useEffect runs after render, for synchronising with something outside React: a subscription, a timer, a network request, the document title. If you are only computing a value from props and state, you do not need an effect — just compute it.',
            'The dependency array says when to re-run. Omit it and the effect runs after every render; an empty array runs it once; listing values re-runs when they change. Leaving out a value the effect actually uses is the classic stale-closure bug: the effect keeps seeing the first render\'s value forever.',
            'Return a cleanup function for anything that needs undoing — clearInterval, removeEventListener, aborting a request. It runs before the next effect and on unmount. Without it, a component that mounts and unmounts repeatedly leaks a listener each time.',
            'A custom hook is just a function starting with "use" that calls other hooks. That is the whole mechanism, and it is how you share stateful logic between components without wrappers.',
          ],
        },
        {
          id: 'react_hooks.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('An effect uses `userId` but the dependency array is empty. What happens?',
              ['It keeps using the first render\'s userId forever', 'It re-runs on every render', 'React throws', 'It works correctly'],
              0,
              'The closure captured the first value and nothing tells it to refresh. This is the stale closure bug.'),
            q('What is the cleanup function for?',
              ['Undoing subscriptions, timers and requests before the next run or unmount', 'Resetting state', 'Freeing memory manually', 'Cancelling the render'],
              0,
              'Anything the effect started that outlives a render has to be stopped, or it accumulates.'),
            q('You derive a filtered list from props inside useEffect and store it in state. Better?',
              ['Compute it during render — no effect or state needed', 'Add it to the dependency array', 'Use useMemo and an effect', 'Use useRef'],
              0,
              'An effect that only syncs derived state adds a render and a chance to be out of date.'),
          ],
        },
      ],
    },

    {
      id: 'react_routing',
      name: 'Routing',
      category: 'Frontend',
      difficulty: 3,
      estimatedHours: [2, 4],
      requires: [{ skillId: 'react_hooks', minLevel: 2 }],
      description: 'Client-side routes, params and navigation without a page reload.',
      why: 'Anything past a single screen needs URLs that work — shareable, bookmarkable, and correct on the back button.',
      activities: [
        {
          id: 'react_routing.learn',
          kind: 'learn',
          title: 'The URL is state',
          body: [
            'A client-side router swaps components based on the URL without a network round trip, using the History API to keep the address bar honest. The URL becomes a piece of application state — one that survives refresh and can be shared.',
            'Route params carry data: /skill/:id gives you the id. Query strings carry optional state — filters, search terms, page numbers. Putting a filter in the URL rather than in component state is what makes a filtered view linkable.',
            'The back button must work. If a modal changes what is on screen but not the URL, back leaves the page entirely, which users read as the app losing their place.',
            'Client routing needs server cooperation: a deep link to /skill/42 must serve the app rather than 404, which is the "works locally, breaks in production" bug everyone meets once.',
          ],
        },
        {
          id: 'react_routing.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('A filter should be shareable by link. Where does it belong?',
              ['In the URL query string', 'In component state', 'In localStorage', 'In context'],
              0,
              'If it is in the URL, copying the address reproduces the view. Nothing else does that.'),
            q('/skill/42 works locally but 404s in production. Why?',
              ['The server does not know to serve the app for unknown paths', 'The route is misspelled', 'The router needs a rebuild', 'History API is unsupported'],
              0,
              'The dev server falls back to index.html; production has to be configured to do the same.'),
            q('Why does client-side routing avoid a full reload?',
              ['It swaps components and updates the URL via the History API', 'It caches the pages', 'It preloads every route', 'It uses an iframe'],
              0,
              'No document request means no white flash and no lost state.'),
          ],
        },
      ],
    },

    {
      id: 'typescript',
      name: 'TypeScript',
      category: 'Programming',
      difficulty: 4,
      estimatedHours: [6, 10],
      requires: [
        { skillId: 'js_objects', minLevel: 3 },
        { skillId: 'react_state', minLevel: 2 },
      ],
      description: 'Static types over JavaScript: interfaces, unions, generics and narrowing.',
      why: 'Types turn a class of runtime bug into a red squiggle, and they document the shape of your data where the data is used.',
      activities: [
        {
          id: 'typescript.learn',
          kind: 'learn',
          title: 'Types that catch real bugs',
          body: [
            'TypeScript checks at compile time and disappears at runtime. It cannot validate an API response — it only promises that your code is consistent with the types you declared. A wrong type declaration is a lie the compiler will happily believe.',
            'Union types plus narrowing are where most of the value is. type Result = {ok: true, value: T} | {ok: false, error: string} means checking result.ok tells the compiler which branch you are in, and accessing .value on the error case becomes an error instead of undefined at 2am.',
            'Prefer unknown to any. any switches the checker off entirely and spreads silently through everything it touches; unknown forces you to narrow before use, which is the check you wanted.',
            'Generics are types with parameters. function first<T>(items: T[]): T | undefined keeps the relationship between what goes in and what comes out, instead of flattening it to any.',
          ],
        },
        {
          id: 'typescript.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('You type an API response as User. The API returns something else. What happens?',
              ['Nothing at compile time — types are erased and not validated at runtime', 'A runtime TypeError', 'TypeScript rejects the response', 'The response is coerced'],
              0,
              'Types are a compile-time claim. Runtime data needs a runtime validator.'),
            q('Why prefer unknown over any?',
              ['unknown forces you to narrow before use; any disables checking', 'unknown is faster', 'any is deprecated', 'They are the same'],
              0,
              'any silently spreads through every expression it touches, taking the checking with it.'),
            q('What does a discriminated union give you?',
              ['Checking one field narrows the type of the rest', 'Faster runtime dispatch', 'Automatic validation', 'Smaller bundles'],
              0,
              'It makes impossible states unrepresentable and invalid access a compile error.'),
          ],
        },
      ],
    },

    {
      id: 'node_basics',
      name: 'Node.js',
      category: 'Backend',
      difficulty: 3,
      estimatedHours: [4, 7],
      requires: [{ skillId: 'js_async', minLevel: 3 }],
      description: 'JavaScript outside the browser: modules, the filesystem, and a server.',
      why: 'Same language, different environment — and the place where you can finally keep a secret.',
      activities: [
        {
          id: 'node_basics.learn',
          kind: 'learn',
          title: 'JavaScript with a filesystem',
          body: [
            'Node is the same language with different globals. There is no window, no document, no DOM. There is a filesystem, a process, environment variables, and the ability to listen on a port.',
            'That last part is the important one: code here runs on a machine the user does not control, so this is where API keys, database credentials and any rule you actually need enforced belong.',
            'Two module systems coexist. ESM (import/export) is the standard and what new code should use; CommonJS (require) is the older one you will meet in existing projects. Mixing them in one package causes most Node setup pain.',
            'Node is single-threaded like the browser, so blocking work blocks every request. Use the async APIs; a synchronous file read in a request handler stalls the whole server.',
          ],
        },
        {
          id: 'node_basics.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('Why can a secret live on the server but not in front-end code?',
              ['The user never receives the server code; they receive all front-end code', 'Servers encrypt variables', 'Front-end code is minified, not hidden', 'It can, in an env var'],
              0,
              'Anything shipped to a browser is readable, whatever the build tool did to it.'),
            q('A synchronous file read inside a request handler causes what?',
              ['Every other request waits — the loop is blocked', 'Only that request is slow', 'A memory leak', 'Nothing measurable'],
              0,
              'One thread serves everyone. Blocking it blocks all concurrent requests.'),
            q('What is missing in Node compared to a browser?',
              ['document and the DOM', 'Promises', 'JSON', 'setTimeout'],
              0,
              'There is no page. The rest of the language is identical.'),
          ],
        },
      ],
    },

    {
      id: 'rest_apis',
      name: 'Building APIs',
      category: 'Backend',
      difficulty: 4,
      estimatedHours: [4, 8],
      requires: [
        { skillId: 'node_basics', minLevel: 3 },
        { skillId: 'js_apis', minLevel: 2 },
      ],
      description: 'Routes, methods, status codes, validation and error shape.',
      why: 'The contract between front and back end. Get it wrong and every client works around it forever.',
      activities: [
        {
          id: 'rest_apis.learn',
          kind: 'learn',
          title: 'Designing an endpoint',
          body: [
            'Methods carry meaning. GET reads and must not change anything. POST creates. PUT replaces, PATCH updates part. DELETE removes. A GET that mutates will eventually be triggered by a crawler or a prefetch, and then it is your outage.',
            'Status codes are the API telling the caller what happened without parsing the body: 200 fine, 201 created, 400 your request was malformed, 401 not authenticated, 403 authenticated but not allowed, 404 not there, 409 conflict, 500 we broke.',
            'Validate every input on the server, and never trust an id from the client to mean permission. "Delete order 812" must check that this user owns order 812 — the single most common real-world API vulnerability is an endpoint that checks you are logged in but not that the thing is yours.',
            'Return errors in a consistent shape. A client that has to handle four different error formats will handle none of them well.',
          ],
        },
        {
          id: 'rest_apis.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('Authenticated user requests DELETE /orders/812, which belongs to someone else. Correct handling?',
              ['Check ownership and refuse — authentication is not authorisation', 'Delete it; they are logged in', 'Return 401', 'Return 200 and ignore it'],
              0,
              'Knowing who someone is says nothing about what they may touch. This gap is the most exploited API flaw there is.'),
            q('Which status code for a well-formed request the user is not permitted to make?',
              ['403', '400', '401', '404'],
              0,
              '401 means "who are you"; 403 means "I know who you are and no".'),
            q('Why must GET never modify data?',
              ['Crawlers, prefetchers and caches issue GETs without user intent', 'It is slower', 'GET cannot carry a body', 'It breaks CORS'],
              0,
              'Anything that can be triggered by a link being seen must be safe.'),
          ],
        },
      ],
    },

    {
      id: 'databases',
      name: 'Databases',
      category: 'Backend',
      difficulty: 4,
      estimatedHours: [5, 9],
      requires: [{ skillId: 'rest_apis', minLevel: 2 }],
      description: 'Tables, relations, queries, indexes and the N+1 problem.',
      why: 'Where the data actually lives. Schema decisions are the hardest to reverse in any application.',
      activities: [
        {
          id: 'databases.learn',
          kind: 'learn',
          title: 'Relations and the N+1',
          body: [
            'A relational database stores rows in tables with a fixed schema. Relations are expressed with foreign keys, and joins reassemble them at query time. The schema is enforced by the database, so bad data cannot get in through a route you forgot to validate.',
            'Normalise first: store each fact once. A user\'s email lives in the users table and nowhere else, so changing it changes it everywhere by construction.',
            'The N+1 problem is the performance bug you will meet first. Fetch 50 posts, then loop and fetch each author: 51 queries where 2 would do. Every ORM has a way to load the relation up front, and knowing it exists is most of the fix.',
            'An index makes lookups on a column fast and writes slightly slower. Any column you regularly filter or join on wants one; a table scan over a million rows is the difference between 2ms and 2 seconds.',
          ],
        },
        {
          id: 'databases.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('You fetch 50 posts then query each post\'s author in a loop. What is this?',
              ['The N+1 problem — 51 queries instead of 2', 'Normalisation', 'A join', 'Correct and idiomatic'],
              0,
              'Load the relation in the first query instead of once per row.'),
            q('Why store a user\'s email in exactly one table?',
              ['One row to update, so it cannot disagree with itself', 'It saves disk space', 'Databases require it', 'It makes joins faster'],
              0,
              'Duplicated facts drift apart. That is the entire argument for normalisation.'),
            q('What does adding an index cost?',
              ['Slightly slower writes and more storage', 'Nothing', 'Slower reads', 'Data integrity'],
              0,
              'The index has to be maintained on every insert and update, which is why you index deliberately rather than everywhere.'),
          ],
        },
      ],
    },

    {
      id: 'auth',
      name: 'Authentication',
      category: 'Backend',
      difficulty: 4,
      estimatedHours: [4, 7],
      requires: [{ skillId: 'databases', minLevel: 2 }],
      description: 'Sessions, tokens, password storage and the difference from authorisation.',
      why: 'The one area where a plausible-looking implementation can be catastrophically wrong.',
      activities: [
        {
          id: 'auth.learn',
          kind: 'learn',
          title: 'Identity, then permission',
          body: [
            'Authentication is who you are. Authorisation is what you may do. They are separate checks and conflating them is how an endpoint ends up letting any logged-in user read anyone\'s data.',
            'Never store a password. Store a hash from a slow, salted algorithm built for the job — bcrypt, scrypt or argon2. A general-purpose hash like SHA-256 is far too fast: modern hardware tries billions per second, which is exactly what you are defending against.',
            'Sessions keep state on the server and hand out an opaque id; tokens carry signed claims and need no server lookup. Sessions are trivial to revoke. Tokens are not — a stolen one stays valid until it expires, which is why token lifetimes are short and refresh tokens exist.',
            'Put session cookies behind HttpOnly, Secure and SameSite. HttpOnly is what stops a script reading the cookie — which is why "store the token in localStorage" is worse than it looks: any XSS on your page can read it.',
          ],
        },
        {
          id: 'auth.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('Why is SHA-256 a poor choice for password hashing?',
              ['It is fast, so brute-forcing is cheap', 'It is not secure', 'It cannot be salted', 'It produces collisions'],
              0,
              'Password hashes should be deliberately slow. bcrypt, scrypt and argon2 are built for that.'),
            q('What does HttpOnly on a cookie prevent?',
              ['JavaScript from reading it, limiting XSS damage', 'Sending it over HTTP', 'Cross-site requests', 'Expiry'],
              0,
              'It is why a cookie can be safer than localStorage for a session token.'),
            q('Main practical difference between sessions and tokens?',
              ['Sessions are easy to revoke; tokens stay valid until expiry', 'Tokens are more secure', 'Sessions cannot scale', 'Tokens do not expire'],
              0,
              'Revocation is the trade-off you are making when you choose stateless tokens.'),
          ],
        },
      ],
    },

    {
      id: 'testing',
      name: 'Testing',
      category: 'Tools',
      difficulty: 3,
      estimatedHours: [3, 6],
      requires: [{ skillId: 'js_arrays', minLevel: 3 }],
      description: 'Unit, integration and end-to-end tests, and what is worth testing.',
      why: 'Tests are what let you change code you wrote six months ago without dread.',
      activities: [
        {
          id: 'testing.learn',
          kind: 'learn',
          title: 'What is worth testing',
          body: [
            'Test behaviour, not implementation. A test that asserts a function was called breaks when you refactor and passes when the feature is broken. A test that asserts the output is right survives the refactor and catches the bug.',
            'The pyramid, roughly: many fast unit tests on pure logic, fewer integration tests over modules working together, a handful of end-to-end tests on the critical path. Inverting it gives a suite that is slow, flaky, and eventually ignored.',
            'Highest value per line: pure functions holding real rules — pricing, permissions, progression, validation. They are trivial to test and expensive to get wrong.',
            'Coverage measures which lines ran, not whether they were checked. 100% coverage with weak assertions proves only that nothing threw.',
          ],
        },
        {
          id: 'testing.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('A test asserts an internal helper was called. What is wrong with it?',
              ['It tests implementation, so refactoring breaks it while bugs pass', 'It is too slow', 'It needs mocks', 'Nothing'],
              0,
              'Tests should pin down behaviour. Anything else is friction with no safety.'),
            q('What does 100% coverage prove?',
              ['Every line ran — not that it was correct', 'The code is bug-free', 'All branches are handled', 'Tests are well written'],
              0,
              'A test with no assertions still produces coverage.'),
            q('Which code is the best value to unit test?',
              ['Pure functions holding business rules', 'CSS-heavy components', 'Third-party libraries', 'Getters and setters'],
              0,
              'Cheap to test, expensive to get wrong, and they rarely change shape.'),
          ],
        },
      ],
    },

    {
      id: 'accessibility',
      name: 'Accessibility',
      category: 'Frontend',
      difficulty: 3,
      estimatedHours: [3, 5],
      requires: [
        { skillId: 'semantic_html', minLevel: 3 },
        { skillId: 'js_events', minLevel: 2 },
      ],
      description: 'Keyboard operation, focus, contrast, and ARIA used sparingly.',
      why: 'It is a legal requirement in many places, and it makes the interface better for everyone using it in a hurry.',
      activities: [
        {
          id: 'accessibility.learn',
          kind: 'learn',
          title: 'Keyboard first',
          body: [
            'The fastest useful audit: put the mouse down and tab through. Can you reach everything interactive? Can you see where you are? Can you operate it with Enter and Space? Can you escape a modal? Most failures show up in the first minute.',
            'The first rule of ARIA is not to use ARIA. A native <button> is focusable, operable and announced correctly for free; a <div role="button"> needs tabindex, key handlers, and still behaves differently. Reach for ARIA only when no native element does the job.',
            'Never remove focus outlines without replacing them. :focus-visible gives you a ring for keyboard users and none for mouse clicks, which is the behaviour people are usually reaching for when they delete the outline.',
            'Do not encode meaning in colour alone. A red border for an invalid field is invisible to a colour-blind user and to anyone in bright sunlight — pair it with an icon or text.',
          ],
        },
        {
          id: 'accessibility.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('Fastest way to catch most accessibility problems?',
              ['Tab through the page with no mouse', 'Run a colour contrast checker', 'Add ARIA roles', 'Test with a screen reader for an hour'],
              0,
              'Keyboard traps, unreachable controls and invisible focus all surface immediately.'),
            q('Why prefer <button> over <div role="button">?',
              ['Focus, keyboard operation and announcement come for free', 'It is shorter to type', 'ARIA is deprecated', 'divs cannot be styled'],
              0,
              'Every ARIA role you add is behaviour you now have to implement yourself.'),
            q('A form marks errors with a red border only. Problem?',
              ['Colour alone is invisible to some users and in some conditions', 'Red is aggressive', 'Borders are hard to see', 'None'],
              0,
              'Status needs a second channel — an icon, text, or both.'),
          ],
        },
      ],
    },

    {
      id: 'performance',
      name: 'Web Performance',
      category: 'Frontend',
      difficulty: 4,
      estimatedHours: [3, 6],
      requires: [
        { skillId: 'react_hooks', minLevel: 2 },
        { skillId: 'js_apis', minLevel: 2 },
      ],
      description: 'Bundle size, rendering cost, images and measuring before optimising.',
      why: 'Speed is a feature, and on a mid-range phone the difference is not subtle.',
      activities: [
        {
          id: 'performance.learn',
          kind: 'learn',
          title: 'Measure, then fix',
          body: [
            'Optimise what you measured. The bottleneck is rarely where it feels like it is, and a day spent memoising components that were never the problem is a day gone.',
            'For most sites the biggest wins are unglamorous: images that are too large, JavaScript that is not needed for the first screen, and fonts that block rendering. Those three outrank almost any algorithmic change.',
            'In React, the usual cause of jank is re-rendering a large subtree from state that lives too high up. Moving state closer to where it is used beats memoising the consequences of it being far away.',
            'Test on a throttled connection and a mid-range device. A fast laptop on office wifi hides every performance problem you have.',
          ],
        },
        {
          id: 'performance.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('First step when a page feels slow?',
              ['Profile it and find the actual bottleneck', 'Memoise components', 'Add lazy loading', 'Reduce bundle size'],
              0,
              'Every other option is a guess until you have measured.'),
            q('A React app janks when typing in a search box. Likely cause?',
              ['State too high up, re-rendering a large subtree per keystroke', 'Too many components', 'A slow regular expression', 'Missing keys'],
              0,
              'Moving the state down usually beats memoising everything below it.'),
            q('Why test on a throttled connection?',
              ['Fast local conditions hide the problems real users hit', 'It is more accurate', 'It reduces cost', 'Browsers behave differently'],
              0,
              'Nothing on your machine resembles a mid-range phone on mobile data.'),
          ],
        },
      ],
    },

    {
      id: 'deployment',
      name: 'Deployment',
      category: 'Tools',
      difficulty: 3,
      estimatedHours: [2, 5],
      requires: [
        { skillId: 'git', minLevel: 3 },
        { skillId: 'react_routing', minLevel: 2 },
      ],
      description: 'Builds, environments, environment variables and shipping.',
      why: 'Code that is not deployed does not count.',
      activities: [
        {
          id: 'deployment.learn',
          kind: 'learn',
          title: 'From repository to URL',
          body: [
            'A build turns your source into what the browser gets: bundled, minified, hashed filenames for caching. What you deploy is the build output, never the source.',
            'Configuration belongs in environment variables, not in the code — same build, different settings per environment. Remember that anything prefixed for the client is compiled into the bundle and therefore public, whatever it is named.',
            'Deploy on every merge to main, from CI, with the same command every time. Manual deploys drift and the failure always arrives on a Friday.',
            'Have a way back. A rollback you have actually tested is worth more than a deploy process you trust.',
          ],
        },
        {
          id: 'deployment.quiz',
          kind: 'quiz',
          title: 'Check yourself',
          questions: [
            q('A client-side env var holds an API secret. Is it safe?',
              ['No — it is compiled into the bundle and readable by anyone', 'Yes, env vars are private', 'Only if the build is minified', 'Only in production mode'],
              0,
              'The prefix that exposes it to your code exposes it to every visitor.'),
            q('Why deploy from CI rather than a laptop?',
              ['The same steps run every time, from a known state', 'It is faster', 'It uses less bandwidth', 'Laptops cannot deploy'],
              0,
              'Manual steps drift, and the drift is invisible until it breaks.'),
            q('What is worth more than a deploy process you trust?',
              ['A rollback you have tested', 'More staging environments', 'Longer QA', 'Blue-green deploys'],
              0,
              'Everything ships broken eventually. What matters is how fast you can undo it.'),
          ],
        },
      ],
    },

    {
      id: 'fullstack',
      name: 'Full Stack Developer',
      category: 'Milestone',
      difficulty: 5,
      estimatedHours: [10, 20],
      requires: [
        { skillId: 'react_routing', minLevel: 3 },
        { skillId: 'typescript', minLevel: 3 },
        { skillId: 'auth', minLevel: 3 },
        { skillId: 'deployment', minLevel: 3 },
        { skillId: 'testing', minLevel: 2 },
      ],
      description: 'Ship a complete application: interface, API, database, auth and deployment.',
      why: 'The capstone. Not a new technique — proof you can carry a real feature across every layer without leaving a hole.',
      activities: [
        {
          id: 'fullstack.project',
          kind: 'project',
          title: 'Ship something end to end',
          brief: 'Build and deploy an application with accounts, persistent data owned per user, a typed API you wrote, and at least one test covering a real rule. Then write down what broke between working locally and working deployed.',
          checklist: [
            'A user can sign up, sign out and sign back in to their own data',
            'Data survives a server restart, not just a refresh',
            'The API rejects a request for another user\'s record — tested, not assumed',
            'No secret is readable in the browser bundle or network tab',
            'It runs at a public URL from a clean clone and a documented command',
            'At least one test covers a rule that would be expensive to get wrong',
          ],
        },
        {
          id: 'fullstack.mastery',
          kind: 'mastery',
          title: 'Explain your own architecture',
          brief: 'Without notes: describe how a single click in your interface reaches the database and comes back. Name every layer it crosses, what could fail at each, and what the user sees when it does.',
          checklist: [
            'Traced the full path: event, request, route, validation, query, response, render',
            'Named a realistic failure at each layer and its user-visible symptom',
            'Identified where authorisation is enforced and why it is that layer',
            'Explained one decision you would make differently next time',
          ],
        },
      ],
    },
  ],
};
